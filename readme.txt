Для ДЗ №2 создала структуру папок как я это поняла.
Также создан файл .yml и Dockerfile.

Созданы пустые файлы main.py, nginx.conf, requirements.txt и .env.

В папки postgres и RabbitMQ положила по пустому текстовому файлу, чтобы Git их запушил на GitHub.

Нужно ли .env передать в gitignore?